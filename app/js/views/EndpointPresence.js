(function ($, App) {

    'use strict';

    /*App.views.EndpointPresence = $.View.extend({

        el: 'body',
        template: 'endpoint-presence',

        events: {
            'change .user-status-dropdown__status__select': function (e) {

                var $el = $(e.target),
                    val = $el.val();

                this.el.find('#user-status-dropdown__status')
                    .text(val)
                    .attr('class', 'user-status-dropdown__status--' + val);

                this.setPresence({
                    presence: val
                });

            }
        },

        presenceTypes: [
            'available',
            'away',
            'busy',
            'be right back',
            'do not disturb',
            'unavailable'
        ],

        render: function () {
        }

    });*/

}(jQuery, App));