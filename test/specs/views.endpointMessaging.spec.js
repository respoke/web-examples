describe('The Endpoint Messaging View', function () {

    beforeEach(function () {
        this.ctrl = App.controllers.messagingCtrl({
            username: 'bob',
            connectTo: 'alice'
        });
    });
    
});