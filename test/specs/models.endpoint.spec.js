describe('The Endpoint Model', function () {

    beforeEach(function () {
        this.endpoint = Respoke.models.Endpoint;
    });

    describe('when an endpoint is created', function () {
        it('should maintain the endpoint state', function () {
            assert.ok(1);
        });
    });

});